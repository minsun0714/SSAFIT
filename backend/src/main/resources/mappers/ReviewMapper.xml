<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.ssafit.dao.ReviewMapper">

    <!-- 리뷰 생성 -->
    <insert id="insertReview" parameterType="Review">
        INSERT INTO review (review_id, member_id, video_id, content, created_at, rating)
        VALUES (#{reviewId}, #{memberId}, #{videoId}, #{content}, now(), #{rating});
    </insert>

    <!-- 특정 영상에 대한 리뷰 조회 -->
    <select id="selectReviewsByVideoId" resultType="Review">
        SELECT * FROM review WHERE video_id = #{videoId};
    </select>

    <!-- 리뷰 수정 -->
    <update id="updateReview" parameterType="Review">
        UPDATE review
        SET content = #{content},
        rating = #{rating}
        WHERE review_id = #{reviewId};
    </update>

    <!-- 리뷰 삭제 -->
    <delete id="deleteReview" parameterType="String">
        DELETE FROM review WHERE review_id = #{reviewId};
    </delete>

    <!-- 리뷰 ID로 리뷰 조회 -->
    <select id="selectReviewById" resultType="com.ssafy.ssafit.domain.Review">
        SELECT * FROM review WHERE review_id = #{reviewId};
    </select>

</mapper>
